#define TYPE long int
#define FORMAT "%li"
#define EQ(a, b) ((a) == (b))
#define LT(a, b) ((a) < (b))
#define ZERO ((TYPE)0)
#define ONE ((TYPE)1)
#define ADD(a, b) ((a) + (b))
#define MINUS(a) (-(a))

